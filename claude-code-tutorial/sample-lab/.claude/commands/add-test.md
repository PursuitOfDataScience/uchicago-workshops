---
description: Write a pytest test for a function the user names
argument-hint: <function name in analysis/>
allowed-tools: Read, Edit(analysis/**)
---
Write a focused pytest test for the function `$ARGUMENTS` defined in `analysis/`.

- First read the function's source and docstring.
- Cover the normal case and at least one edge case (empty input, single element, or a boundary).
- Put the test in the matching `analysis/test_*.py` file; do not modify existing tests.
- Do not change the function under test.
