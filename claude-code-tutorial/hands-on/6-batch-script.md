# Task 6 · Write a batch job script

> Write me a Slurm batch script that runs `python train.py` in the `gpu` partition on 1 GPU, with 8
> CPUs, 32 GB of memory, and a 4-hour time limit; emails me when it finishes; and sends output to a
> log file named after the job. Save it as `train.sbatch`, explain every `#SBATCH` line in plain
> English, and show me the command to submit it — but don't submit it yourself.
