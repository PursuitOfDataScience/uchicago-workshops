# Bonus · Run it headless

Run this in a terminal (in this folder):

```bash
claude -p "Read the penguins CSV at https://raw.githubusercontent.com/mwaskom/seaborn-data/master/penguins.csv and reply with only the mean body_mass_g for each species, as compact JSON." --output-format json --model haiku
```
