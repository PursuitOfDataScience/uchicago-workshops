# Task 6 · Make a figure

> Write and run a Python script that saves a boxplot of `body_mass_g` by species to
> `body_mass_by_species.png` in this folder — this is a headless node with no display, so save
> the file rather than showing it. Label the axes and give it a title, then tell me what the
> plot shows.
